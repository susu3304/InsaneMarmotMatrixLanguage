# Pack Law Notes

Packaging is platform-facing, so it is covered by CLI smoke tests instead of
IMM probe blocks. The compatibility rule is that a packed Python pelt must run
the same entry source and produce the same output as `imm run`.
